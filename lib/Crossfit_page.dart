import 'package:flutter/material.dart';

class CrossFitPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Cross-Fit")),
      body: Center(child: Text("Cross-Fit Page")),
    );
  }
}
