import 'package:flutter/material.dart';
import 'weight_training_page_1.dart';
import 'weight_training_page_2.dart';
import 'select_exercise_page.dart';
import 'Running_page.dart';
import 'Crossfit_page.dart';
import 'start_exercise_page.dart';
import 'finish_workout_page.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Workout App',
      home: Weight_Training_Page_1(),
      routes: {
        '/1': (context) => RunningPage(),
        '/2': (context) => Weight_Training_Page_1(),
        '/3': (context) => CrossFitPage(),
        //'/4': (context) => StartExercisePage(), //이 ㄱㅅㄲ;;;;;;아오
        '/5': (context) => Weight_Training_Page_2(),
        '/6': (context) => SelectExercisePage(),
        '/7': (context) => finish_workout_page(),
      },
    );
  }
}

/*  페이지 템플릿 
class New_Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("New")),
      body: Center(child: Text("New Page")),
    );
  }
}
*/
